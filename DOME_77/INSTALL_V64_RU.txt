DOME v64 — постоянная production установка\n\n1) ЛОКАЛЬНАЯ ПРОВЕРКА (PowerShell, из распакованной папки):\n\ndocker compose down\ndocker compose build --no-cache\ndocker compose up -d\ndocker compose ps\ndocker compose logs --tail=100\n\n2) ПЕРЕД RAILWAY DEPLOY ОБЯЗАТЕЛЬНО ОСТАНОВИТЬ ЛОКАЛЬНЫЙ BOT:\n\ndocker compose down\n\n3) ЕСЛИ ВЫ РАЗВОРАЧИВАЕТЕ ЧЕРЕЗ GITHUB И РЕПОЗИТОРИЙ УЖЕ КЛОНИРОВАН:\n\ngit add .\ngit commit -m "DOME v64 production Railway + Mini App + cartoon delivery fix"\ngit push origin main\n\n4) RAILWAY VARIABLES ДОЛЖНЫ СОДЕРЖАТЬ КАК МИНИМУМ:\nBOT_TOKEN\nOPENAI_API_KEY\n\nRAILWAY_PUBLIC_DOMAIN Railway выставляет автоматически после Generate Domain. WEBAPP_BASE_URL можно оставить пустым. PORT Railway выставляет автоматически.\n\n5) ПОСЛЕ DEPLOY ПРОВЕРИТЬ:\n- Railway deployment = Active/Online\n- /health отвечает\n- Telegram /start отвечает без TelegramConflictError\n- слайд-чемодан открывает Mini App\n- после 34/34 приходит MP4\n