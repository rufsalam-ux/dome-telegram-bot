# DOME v55 — системный фикс урока

Исправлено:
- изоляция контекста каждого слайда;
- AI не выбирает следующий вопрос и не придумывает новые темы;
- после animal compare обязательная реплика строится про фактически выбранное правильное животное;
- только пары penguin/parrot и lion/turtle;
- добавлена отдельная реплика turtle;
- обязательная реплика не более 3 голосовых записей;
- убрано слоговое дробление в подсказках;
- перевод сохраняет имена персонажей;
- обновлён стиль TTS и cache key;
- новый одиночный penguin.png встроен в проект;
- финальный титр добавляется отдельным 3-секундным кадром в MP4;
- добавлен движок overlay для текста внутри мультфильма. Для реального перекрытия встроенного русского текста нужно заполнить точные секунды/координаты в content/lessons/demo_001/cartoon_text_overlays.json. Они не выдумываются автоматически.
- локализация слайдов уже использует AI image edit + cache через visual_localization.py.

Важно: cartoon_text_overlays.json оставлен без фиктивных координат. Как только зоны текста размечены, движок применит их для target_language != ru.
