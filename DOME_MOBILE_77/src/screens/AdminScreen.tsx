import React from 'react';import { ScrollView } from 'react-native';import { Body,Button,Card,H1,H2 } from '../components/Ui';import { useAppStore } from '../store/AppStore';
export function AdminScreen(){const s=useAppStore();return <ScrollView contentContainerStyle={{padding:24}}><H1>Administrator</H1><Card><H2>Commercial dashboard</H2><Body>Registrations · verified parents · paid users · MRR · refunds/chargebacks · active subscriptions · lessons completed · movies delivered · homework delivered.</Body></Card><Card><H2>Evidence bundle</H2><Body>For each parent: phone/email verification, accepted document versions + hashes + language + UTC timestamps, order details, provider transaction IDs, lesson start/completion events, movie/email delivery and homework delivery.</Body></Card><Card><H2>Child privacy</H2><Body>Voice deletion · movie deletion · child-data deletion · export requests · consent withdrawal · retention jobs.</Body></Card><Button title='Back' secondary onPress={()=>s.setScreen('home')}/></ScrollView>}
